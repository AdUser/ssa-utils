[алгоритм кодирования данных]
Для встраивания файла используется немного измененное uue-кодирование: "алфавит" сдвинут на один символ (+33 вместо +32 в стандартном алгоритме).
Также, способ записи данных очень упрощен:
  * нет заголовка (begin <file mode> <filename> ...),
  * окончания (end ...),
  * последней строки с одним пробелом (`),
  * и числа закодированыых символов в начале строки.
Каждая строка, за исключение последней - ровно 80 символов (видимо под размер стандартного терминала).
Если количество входных байт не кратно 3м - блок дополняется нулевыми байтами (0x0).
